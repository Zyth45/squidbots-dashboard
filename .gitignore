# Your own settings: paths, ports, and where the public copy goes.
dashboard.json

# The Discord webhook, which is a secret.
alerte-discord.txt

# Written while it runs.
baseline.json
history.json
xp-history.json
*.log
*.tmp
levels.json

# Backups the Settings page takes before it writes a .conf.
config-backups/

# Maps extracted from your own client by tools/gen_art.py: Blizzard's, kept local, never committed
# or published. ui/ is interface art from an earlier version, no longer used.
maps/
ui/

__pycache__/
