# Your own settings: paths, ports, and where the public copy goes.
dashboard.json

# The Discord webhook, which is a secret.
alerte-discord.txt

# Written while it runs.
baseline.json
history.json
xp-history.json
*.log
*.tmp
levels.json

# Backups the Settings page takes before it writes a .conf.
config-backups/

# Blizzard's art, extracted from your own client by tools/gen_mapart.py and
# tools/gen_uiart.py. Kept local, never committed.
maps/
ui/

__pycache__/
